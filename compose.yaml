services:
  frontend:
    build: 
      context: . 
      dockerfile: Dockerfile

    container_name: frontend-F1-app

    ports: 
    - "5173:5173"

    volumes: 
    - .:/app
    - node_modules:/app/node_modules

    command: npm run dev
  
volumes:
  node_modules: